# Unused Data

現在の分析Notebookから参照されていないデータを置く。

ここにあるファイルは、削除候補ではなく「今の運用では使っていない」データ。

使う分析が始まったら、目的に合わせて `data/candidates/`、`data/processed/`、または新しい専用フォルダへ移す。

## csv

GPSデータ、コンディションデータなど、現行のトラッキングXML分析では使っていないCSV。

## 99_old_papers_dir

旧 `data/論文/` フォルダ。PDF本体は `data/98_reference/papers/` に移動済み。
